<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-fill min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end w-full">
        <a
          href="/logout"
          class="bg-red-500 rounded-md px-3 py-2 test-sm inline-block mb-5"
          >Logout</a
        >
      </div>
      <div class="w-full">
        <h3 class="text-3xl">
          <span class="font-light">Hello</span>, <%= user.name %>
        </h3>
        <h5 class="mb-5">Edit post.</h5>
        <form action="/update/<%=post._id%>" method="Post">
          <textarea
            style="height: 120px"
            class="resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none p-2 w-1/3"
            name="post"
          >
<%= post.content.trim() %>
            </textarea
          >
          <input
            style="cursor: pointer"
            type="submit"
            class="px-3 py-2 bg-yellow-500 block rounded-md mt-2 text-sm text-black"
            value="Update post"
          />
        </form>
      </div>
    </div>
  </body>
</html>
